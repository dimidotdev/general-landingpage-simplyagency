---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Contato">
  <main class="container">
    <section class="contact-header text-center">
      <h1>Fale Conosco</h1>
      <p class="lead">Tem um projeto em mente ou alguma dúvida? Adoraríamos ouvir você.</p>
    </section>

    <section class="contact-content">
      <div class="contact-form-wrapper">
        <h2>Envie sua Mensagem</h2>
        <form id="contact-form" action="YOUR_FORM_ENDPOINT" method="POST"> {/* Replace with actual endpoint or use JS */}
          <div class="form-group">
            <label for="name">Nome</label>
            <input type="text" id="name" name="name" required>
          </div>
          <div class="form-group">
            <label for="email">Email</label>
            <input type="email" id="email" name="email" required>
          </div>
          <div class="form-group">
            <label for="subject">Assunto</label>
            <input type="text" id="subject" name="subject" required>
          </div>
          <div class="form-group">
            <label for="message">Mensagem</label>
            <textarea id="message" name="message" rows="5" required></textarea>
          </div>
          <button type="submit">Enviar Mensagem</button>
        </form>
      </div>

      <div class="contact-info">
        <h2>Informações de Contato</h2>
        <p>Entre em contato diretamente ou visite-nos:</p>
        <ul>
          <li><strong>Email:</strong> <a href="mailto:contato@suaempresa.com">contato@suaempresa.com</a></li>
          <li><strong>Telefone:</strong> <a href="tel:+5511999999999">+55 (11) 99999-9999</a></li>
          <li><strong>Endereço:</strong> Rua Exemplo, 123, Sala 45, Cidade - Estado, CEP 00000-000 (Se aplicável)</li>
        </ul>
        <!-- Optional Map -->
        <!-- <div class="map-container">
          <iframe src="GOOGLE_MAPS_EMBED_URL" width="100%" height="300" style="border:0;" allowfullscreen="" loading="lazy" referrerpolicy="no-referrer-when-downgrade"></iframe>
        </div> -->
      </div>
    </section>
  </main>
</Layout>

<style>
  .contact-header {
    margin-bottom: var(--space-xl);
  }

  .lead {
    font-size: 1.2rem;
    color: var(--color-text-secondary);
    max-width: 600px;
    margin: 0 auto;
  }

  .contact-content {
    display: grid;
    grid-template-columns: 2fr 1fr; /* Form takes more space */
    gap: var(--space-xl);
  }

  .contact-form-wrapper, .contact-info {
    background-color: var(--color-surface);
    padding: var(--space-lg);
    border-radius: var(--radius-lg);
  }

  .contact-form-wrapper h2, .contact-info h2 {
    margin-bottom: var(--space-lg);
    text-align: center;
  }

  .form-group {
    margin-bottom: var(--space-md);
  }

  .form-group label {
    display: block;
    margin-bottom: var(--space-xs);
    font-weight: var(--font-weight-medium);
    color: var(--color-text-secondary);
  }

  .form-group input,
  .form-group textarea {
    width: 100%;
    padding: var(--space-sm);
    background-color: var(--color-background); /* Slightly darker than surface */
    border: 1px solid var(--color-surface); /* Subtle border */
    color: var(--color-text-primary);
    border-radius: var(--radius-md);
    transition: border-color 0.2s ease, box-shadow 0.2s ease;
  }

  .form-group input:focus,
  .form-group textarea:focus {
    outline: none;
    border-color: var(--color-accent);
    box-shadow: 0 0 0 2px rgba(var(--color-accent), 0.3);
  }

  #contact-form button {
    width: 100%;
    padding: var(--space-sm);
    font-size: 1.1rem;
  }

  .contact-info ul {
    list-style: none;
    padding: 0;
  }

  .contact-info li {
    margin-bottom: var(--space-md);
    color: var(--color-text-secondary);
  }

  .contact-info strong {
    color: var(--color-text-primary);
    margin-right: var(--space-xs);
  }

  .contact-info a {
    color: var(--color-accent);
  }
  .contact-info a:hover {
    text-decoration: underline;
  }

  .map-container {
    margin-top: var(--space-lg);
    border-radius: var(--radius-md);
    overflow: hidden; /* Clip the map to the border radius */
  }

  /* Responsive adjustments */
  @media (max-width: 992px) {
    .contact-content {
      grid-template-columns: 1fr; /* Stack form and info */
    }
  }
</style>
